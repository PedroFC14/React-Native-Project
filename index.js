import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);

// this is the file that launches the app and loads App.js. It is the real
// entry point of the application